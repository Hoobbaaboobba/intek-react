import StorageText from "./StorageText";

const StorageSection = () => {
  return (
    <div className="relative mt-[100px] h-[80dvh] w-full bg-[url('/images/storage-bg.png')] bg-cover">
      <StorageText />
    </div>
  );
};

export default StorageSection;
