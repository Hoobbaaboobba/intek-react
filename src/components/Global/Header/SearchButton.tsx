import { Button } from "@/components/ui/button";
import { Search } from "lucide-react";

const SearchButton = () => {
  return (
    <Button>
      <Search />
    </Button>
  );
};

export default SearchButton;
